{
    hypnotoad => {
        listen => [qw(
            https://*:8443?cert=/etc/ssl/certs/gparse.pem&key=/etc/ssl/private/gparse.pem
            http://*:8080
        )],
        pid_file => '/tmp/gparse.pid',
    },
}
