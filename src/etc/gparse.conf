{
    hypnotoad => {
        listen => [qw(
            https://*:8443?cert=/etc/ssl/certs/gparse.pem&key=/etc/ssl/private/gparse.pem
            http://*:8080
        )],
        pid_file => '/tmp/gparse.pid',
    },

    minify => {
        do_javascript => 'shrink',
        do_stylesheet => 'minify',
        html5 => 1,
        remove_comments => 1,
        remove_newlines => 1,
    },
}
